define(function (require) {
    "use strict";

    var Model;

    Model = Backbone.Model.extend({
        initialize: function () {

        },

        validate: function(attrs, options) {

        }
    });

    return Model;
});